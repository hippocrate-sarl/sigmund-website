source "https://rubygems.org"

# Match all versions on GitHub Pages:
# What is built locally is what will be published.
gem "github-pages", group: :jekyll_plugins

# Since Ruby 3.0, webrick is no longer a default gem,
# and the Jekyll 3.x server requires it.
gem "webrick", "~> 1.8"
